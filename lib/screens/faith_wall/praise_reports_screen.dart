import 'package:flutter/material.dart';

class PraiseReportsScreen extends StatelessWidget {
  const PraiseReportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('4. PRAISE REPORTS')),
      body: Center(
        child: Text('4. PRAISE REPORTS', style: const TextStyle(color: Colors.white, fontSize: 20)),
      ),
    );
  }
}
