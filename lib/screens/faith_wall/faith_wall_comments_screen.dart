import 'package:flutter/material.dart';

class FaithWallCommentsScreen extends StatelessWidget {
  const FaithWallCommentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('6. COMMENTS')),
      body: Center(
        child: Text('6. COMMENTS', style: const TextStyle(color: Colors.white, fontSize: 20)),
      ),
    );
  }
}
